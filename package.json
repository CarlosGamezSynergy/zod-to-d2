{
  "name": "zod-to-d2",
  "version": "0.0.1",
  "description": "A library that extends Zod types and provides tools to convert schemas to D2 diagrams.",
  "main": "index.js",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "publish": "pnpm build && pnpm pack && npm publish",
    "test": "vitest --run"
  },
  "keywords": [],
  "author": "Carlos Gamez",
  "license": "MIT",
  "packageManager": "pnpm@10.13.1",
  "peerDependencies": {
    "zod": "^3.25.0 || ^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.1.0",
    "vitest": "^3.2.4",
    "zod": "^3.25.0 || ^4.0.0"
  },
  "dependencies": {
    "bundle-require": "^5.1.0"
  }
}